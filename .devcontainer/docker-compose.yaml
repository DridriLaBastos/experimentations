services:
  app:
    image: python:3.14.7
    working_dir: /workspace
    volumes:
      - ..:/workspace
    ports:
      - "8000:8000"
    command: >-
      sh -c "pip install -r requirements.txt && python manage.py runserver 0.0.0.0:8000"
