name: cryptalk

networks:
  app:

services:
  db:
    image: postgres:17
    networks:
      - app
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=PgP@ss91!
  pgadmin:
    image: dpage/pgadmin4
    environment:
      - PGADMIN_DEFAULT_EMAIL=adrien.cournand@gmail.com
      - PGADMIN_DEFAULT_PASSWORD=PgP@ss91!
    networks:
      - app
    ports:
      - "5050:80"
    depends_on:
      - db